export default defineAppConfig({
    ui: {
        primary: 'black',
        tooltip: {
            base: 'overflow-scroll'
        },
        container: {
            strategy: 'override',
            constrained: 'max-w-screen-2xl'
        }
    }
});